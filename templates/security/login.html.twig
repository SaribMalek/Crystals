<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login | AS Crystal</title>
    <style>
        :root {
            --bg: #f5f7fb;
            --card: #ffffff;
            --text: #243142;
            --muted: #6b7a90;
            --primary: #1f3b61;
            --primary-dark: #13253f;
            --border: #dbe4f1;
            --error-bg: #fff3f3;
            --error-border: #f4c8c8;
            --error-text: #9d2f2f;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            display: grid;
            place-items: center;
            background: radial-gradient(circle at top right, #eef3fb, var(--bg));
            font-family: "Segoe UI", Tahoma, sans-serif;
            color: var(--text);
            padding: 20px;
        }

        .login-card {
            width: min(460px, 100%);
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: 14px;
            padding: 28px;
            box-shadow: 0 14px 30px rgba(26, 44, 71, 0.08);
        }

        .title {
            margin: 0 0 6px;
            font-size: 1.7rem;
            font-weight: 700;
            color: var(--primary);
        }

        .subtitle {
            margin: 0 0 22px;
            color: var(--muted);
            font-size: 0.94rem;
        }

        .field {
            margin-bottom: 14px;
        }

        label {
            display: block;
            margin-bottom: 6px;
            font-size: 0.86rem;
            font-weight: 600;
            color: #3d4f67;
        }

        input {
            width: 100%;
            height: 44px;
            border: 1px solid #cdd9e9;
            border-radius: 10px;
            padding: 0 12px;
            font-size: 0.95rem;
            color: var(--text);
            background: #fff;
        }

        input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(31, 59, 97, 0.12);
        }

        .error {
            margin-bottom: 14px;
            background: var(--error-bg);
            border: 1px solid var(--error-border);
            border-radius: 10px;
            padding: 10px 12px;
            color: var(--error-text);
            font-size: 0.88rem;
        }

        .btn {
            width: 100%;
            height: 44px;
            border: 0;
            border-radius: 10px;
            background: var(--primary);
            color: #fff;
            font-size: 0.95rem;
            font-weight: 600;
            cursor: pointer;
        }

        .btn:hover {
            background: var(--primary-dark);
        }
    </style>
</head>
<body>
<main class="login-card">
    <h1 class="title">Admin Login</h1>
    <p class="subtitle">Sign in to access the admin dashboard.</p>

    {% if error %}
        <div class="error">Invalid email or password.</div>
    {% endif %}

    <form method="post">
        <div class="field">
            <label for="username">Email</label>
            <input id="username" name="_username" type="email" value="{{ last_username }}" required autofocus>
        </div>

        <div class="field">
            <label for="password">Password</label>
            <input id="password" name="_password" type="password" required>
        </div>

        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

        <button class="btn" type="submit">Login</button>
    </form>
</main>
</body>
</html>
